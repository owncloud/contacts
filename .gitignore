build
js/public
js/vendor
coverage
npm-debug.log
package-lock.json
# js/dav/dav.js is a committed build artifact, so its dependency tree has to be
# locked or a rebuild silently produces a different bundle. See js/dav/Makefile.
!js/dav/package-lock.json

.php_cs.cache
.php-cs-fixer.cache

# Composer
vendor/
vendor-bin/**/vendor
vendor-bin/**/composer.lock

# Mac OS
.DS_Store

# Tests - auto-generated files
/tests/acceptance/output*
/tests/output/
.phpunit.result.cache

# Node.js modules
node_modules/

# SonarCloud scanner
.scannerwork

# drone CI is in .drone.star, do not let someone accidentally commit a local .drone.yml
.drone.yml
# IDE Caches
.idea/
.vscode/
